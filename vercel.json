{
  "buildCommand": "pnpm -w run typecheck:libs && pnpm --filter @workspace/ethiobiz run build && ls -la && ls -la artifacts/ethiobiz",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.ts" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
