{
  "buildCommand": "pnpm -w run typecheck:libs && pnpm --filter @workspace/ethiobiz run build && cp -r artifacts/ethiobiz/dist .",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.ts" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
